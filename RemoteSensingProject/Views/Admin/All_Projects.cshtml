
@{
    ViewBag.Title = "All_Projects";
    Layout = "~/Views/Shared/_adminLayout.cshtml";
    int i = 1;
}
<link href="~/assets/css/ExternalPrjStyle.css" rel="stylesheet" />
<style>
    .link-icon {
        height: 16px;
    }
</style>
<style>
    .progress-bar {
        width: 100% !important;
        background-color: #f3f3f3 !important;
        border-radius: 5px !important;
        height: 20px !important;
    }

    .progress {
        height: 100% !important;
        background-color: #4caf50 !important;
        border-radius: 5px !important;
        text-align: center !important;
        line-height: 20px !important; /* Aligns text in the middle */
        color: white !important;
        justify-content: center;
    }
    /* Custom button styling */
    .dt-button {
        font-size: 14px !important;
        padding: 10px 20px !important;
        border-radius: 4px !important;
        border: none !important;
        cursor: pointer !important;
        margin-right: 5px !important;
        display: inline-block !important;
        font-weight: 600 !important;
        transition: all 0.3s ease-in-out !important;
    }

        /* Excel button (Green) */
        .dt-button.buttons-excel {
            background-color: #3b9f42 !important; /* Excel-like green */
            color: white !important;
        }

            .dt-button.buttons-excel:hover {
                background-color: #2a7c2f !important; /* Darker green on hover */
            }

        /* PDF button (Red) */
        .dt-button.buttons-pdf {
            background-color: #e74c3c !important; /* PDF-like red */
            color: white !important;
        }

            .dt-button.buttons-pdf:hover {
                background-color: #c0392b !important; /* Darker red on hover */
            }

    /* Additional styling for the buttons wrapper */
    .dataTables_wrapper .dt-buttons {
        margin-bottom: 10px !important;
    }
</style>
<div class="search-box p-4 bg-white rounded mx-0 mb-3 box-shadow">
    <form class="forms-sample">
        <div class="row align-items-center">
            <div class="col-lg-2">
                <h5>All Project Lists</h5>
            </div>
            <div class="col-lg-4 col-md-6">
                <input type="text" placeholder="Search by project name" id="searchProjectBy" class="form-control">
            </div>
            <div class="col-lg-3 col-md-6">
                <select class="form-select form-select-lg" id="searchByProjectManager">
                    <option selected value="">Serch by Project Manager</option>
                    @foreach (var item in ViewBag.ManagerList as List<RemoteSensingProject.Models.Admin.main.Employee_model>)
                    {
                        <option value="@item.EmployeeName">@item.EmployeeName</option>
                    }
                </select>
            </div>
            <div class="col-lg-3 col-md-6">
                <select class="form-select form-select-lg" id="filterProjectStatus">
                    <option selected value="">Project Status</option>
                    <option>Work in Progress</option>
                    <option>Not Started</option>
                    <option>Delay</option>
                    <option>Completed</option>
                </select>
            </div>
        </div>
    </form>
</div>
<div class="row mx-0">
    <div class="col-md-12 p-0 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table" id="AllProjectList">
                        <thead>
                            <tr>
                                <th>Sr. No.</th>
                                <th>Project Code</th>
                                <th>Project Name</th>
                                <th>Project Manager</th>
                                <th>Assign Date</th>
                                <th>Start Date</th>
                                <th>Completion Date</th>
                                <th>Physical in %</th>
                                <th>Financial in %</th>
                                <th>Overall in %</th>
                                <th>Project Status</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in ViewBag.ProjectList as List<RemoteSensingProject.Models.Admin.main.Project_model>)
                            {
                                <tr>
                                    <td class="text-center">@i</td>
                                    <td>@item.projectCode</td>
                                    <td>
                                        <a href="#" class="viewProject" data-title="View" data-Id="@item.Id">
                                            @item.ProjectTitle
                                        </a>
                                    </td>
                                    <td>@item.ProjectManager</td>
                                    <td>@Convert.ToDateTime(item.AssignDate).ToString("dd MMM yyyy")</td>
                                    <td>@Convert.ToDateTime(item.StartDate).ToString("dd MMM yyyy")</td>
                                    <td>@Convert.ToDateTime(item.CompletionDate).ToString("dd MMM yyyy")</td>
                                    <td>
                                        <div class="progress-bar">
                                            <div class="progress" style="width:@item.physicalcomplete%; color:black;">@item.physicalcomplete%</div>
                                        </div>
                                    </td>
                                    <td>
                                        <div class="progress-bar">
                                            <div class="progress" style="width:@item.Percentage%; color:black;">@item.Percentage%</div>
                                        </div>
                                    </td>
                                    <td>
                                        <div class="progress-bar">
                                            <div class="progress" style="width:@item.overallPercentage%; color:black;">@item.overallPercentage%</div>
                                        </div>
                                    </td>
                                    <td>
                                        @if (item.ProjectStatus || item.physicalcomplete == 100)
                                        {
                                            <span class="badge bg-success">Completed</span>
                                        }
                                        else if (item.CompletionDate < DateTime.Now)
                                        {
                                            <span class="badge bg-danger">Delay</span>
                                        }
                                        else if (item.StartDate < DateTime.Now)
                                        {
                                            <span class="badge bg-warning">Work in Progress</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-primary">Not Started</span>
                                        }
                                    </td>

                                    <td class="text-center">
                                        <ul class="d-flex list-unstyled mb-0 justify-content-center">
                                            <li class="me-2">
                                                @*<a href="/Admin/View_Weekly_Update?Id=@item.Id" data-title="View Weekly Update">
                                                        <i class="link-icon " data-feather="calendar" style="color:orangered"> </i>
                                                    </a>*@
                                                <a href="#" data-title="Report" class="monthlyReport" data-prjType="@item.ProjectType" data-prjId="@item.Id">
                                                    <i class="link-icon " data-feather="calendar" style="color:orangered"> </i>
                                                </a>
                                            </li>

                                            @*<li class="me-2">
                                                <a href="/Admin/View_Project_Stage?Id=@item.Id" data-title="View Stages">
                                                    <i class="link-icon text-info" data-feather="plus-circle"> </i>
                                                </a>
                                            </li>*@
                                            <li class="me-2">
                                                <a href="/Admin/View_Expenses?Id=@item.Id" data-title="View Expenses">
                                                    <i class="link-icon text-danger" data-feather="file-plus"></i>
                                                </a>
                                            </li>
                                        </ul>
                                    </td>
                                </tr>
                                i++;
                            }

                        </tbody>
                    </table>
                </div>
            </div>
        </div>

        <div class="modal fade" id="viewProjectModal" tabindex="-1" aria-labelledby="viewProjectModal" aria-hidden="true">
            <div class="modal-dialog modal-dialog-scrollable modal-lg modal-dialog-centered">
                <div class="modal-content">
                    <div class="modal-header d-flex">
                        <h1 class="modal-title fs-5 text-success" id="exampleModalLabel"> PPP Based Slum Rehabilitaiton</h1>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
                    <div class="modal-body" id="viewProjectModalBody">
                    </div>
                </div>
            </div>
        </div>


        <div class="modal fade" id="viewMonthlyReportExternalPrj" tabindex="-1" aria-labelledby="viewMonthlyReportExternalPrjLabel" aria-hidden="true">
            <div class="modal-dialog modal-dialog-scrollable modal-xl modal-dialog-centered">
                <div class="modal-content">
                    <div class="modal-header d-flex">
                        <h1 class="modal-title fs-5 text-success" id="viewMonthlyReportExternalPrjLabel">Monthly Report</h1>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
                    <div class="modal-body" id="formToPrintFinancialRprt">
                        <div class="form-container">
                            <div class="row">
                                <div class="col-sm-6 mx-auto text-center">
                                    <span>रूप पत्र-2</span>
                                    <p class="fw-bold" style="font-size: 32px;">रिमोट सेंसिंग एप्लीकेशन सेण्टर , उत्तर प्रदेश बाह्य  सहायतित परियोजनाओं की भौतिक प्रगति</p>
                                </div>
                            </div>
                            <div class="form-title">
                                माह: नवम्बर, 2024
                            </div>

                            <table>
                                <thead>
                                    <tr>
                                        <th rowspan="2" style="width: 5%">क्र.सं.</th>
                                        <th rowspan="2" style="width: 10%">परियोजना का नाम</th>
                                        <th rowspan="2" style="width: 10%">कुल लागत</th>
                                        <th rowspan="2" style="width: 10%">वित्त पोषण संस्था</th>
                                        <th rowspan="2" style="width: 10%">कुल लक्ष्य</th>
                                        <th rowspan="2" style="width: 10%">31.03.2024 तक की उपलब्धि</th>
                                        <th style="width: 25%" colspan="4">वर्ष 2024-25</th>
                                        <th rowspan="2" style="width: 10%">लाभाविंत होने वाले विभाग</th>
                                        <th rowspan="2" style="width: 10%">परियोजना प्रक्षेपण / परियोजना का निष्कर्ष</th>
                                    </tr>
                                    <tr>
                                        <th style="width:5%">लक्ष्य</th>
                                        <th style="width:10%">01.04.2024 से क्रमिक उपलब्धि</th>
                                        <th style="width:5%">माह का लक्ष्य</th>
                                        <th style="width:5%">माह की उपलब्धि</th>
                                    </tr>
                                    <tr>
                                        <th>1</th>
                                        <th>2</th>
                                        <th>3</th>
                                        <th>4</th>
                                        <th>5</th>
                                        <th>6</th>
                                        <th>7</th>
                                        <th>8</th>
                                        <th>9</th>
                                        <th>10</th>
                                        <th>11</th>
                                        <th>12</th>
                                    </tr>
                                </thead>
                                <tbody id="externalPrjTbody">
                                </tbody>
                            </table>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <input type="button" class="btn btn-primary" value="Download" id="printFinancialRprt" />
                    </div>
                </div>
            </div>
        </div>
        <div class="modal fade" id="viewMonthlyReportInternalPrj" tabindex="-1" aria-labelledby="viewMonthlyReportInternalPrjLabel" aria-hidden="true">
            <div class="modal-dialog modal-dialog-scrollable modal-xl modal-dialog-centered">
                <div class="modal-content" id="formToPrintMonthlyRprt">
                    <div class="modal-header d-flex">
                        <h1 class="modal-title fs-5 text-success" id="viewMonthlyReportInternalPrjLabel">Monthly Report</h1>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
                    <div class="modal-body">
                        <div class="form-container">
                            <div class="row">
                                <div class="col-sm-6 mx-auto text-center">
                                    <span>मासिक समीक्षा: रूप पत्र-2</span>
                                    <p class="fw-bold" style="font-size: 15px;">शासन से गैर वेतन मद में प्राप्त धनराशि से संचालित योजना / कार्यक्रम की भौतिक उपलब्धियो का विवरण </p>
                                </div>
                            </div>
                            <div class="d-flex justify-content-between">
                                <div class="form-title">
                                    भू-संसाधन प्रभाग
                                </div>
                                <div class="form-title">
                                    माह: @DateTime.Now.ToString("MMMM"), @DateTime.Now.Year
                                </div>

                            </div>

                            <table>
                                <thead>
                                    <tr>
                                        <th rowspan="2" style="width: 5%">क्र.सं.</th>
                                        <th rowspan="2" style="width: 10%">मद / परियोजन का नाम</th>
                                        <th rowspan="2" style="width: 10%">इकाई</th>
                                        <th colspan="2" style="width: 30%">लक्ष्य</th>
                                        <th colspan="2" style="width: 30%">उपलब्धि</th>
                                        <th rowspan="2" style="width: 10%">प्रदेश सरकार के लाभाविंत होने वाले विभाग</th>
                                        <th rowspan="2" style="width: 10%">अभ्युक्ति</th>
                                    </tr>
                                    <tr>
                                        <th style="width:15%">वार्षिक</th>
                                        <th style="width:15%">आलोच्य मासांत तक</th>
                                        <th style="width:15%">आलोच्य माह में</th>
                                        <th style="width:15%">आलोच्य मासांत तक क्रमिक</th>
                                    </tr>
                                    <tr>
                                        <th>1</th>
                                        <th>2</th>
                                        <th>3</th>
                                        <th>4</th>
                                        <th>5</th>
                                        <th>6</th>
                                        <th>7</th>
                                        <th>8</th>
                                        <th>9</th>
                                    </tr>
                                </thead>
                                <tbody id="internalProjectTbody">
                                </tbody>
                            </table>
                        </div>

                    </div>
                    <div class="modal-footer">
                        <input type="button" class="btn btn-primary" value="Download" id="printMonthlyRprt" />
                    </div>
                </div>
            </div>
        </div>

    </div>
</div>
<script>
    $(".viewProject").click(function () {
        let id = $(this).attr("data-Id");
        Swal.fire({
            title: "Please wait...",
            text: "Processing your request ...",
            allowOutsideClick: false,
            allowEscapeKey: false,
            timer: 100000,
            didOpen: () => {
                Swal.showLoading();
            }
        })
        $.ajax({
            url: "/admin/GetProjecDatatById",
            type: "get",
            data: { Id: id },
            success: function (res) {
                console.log(res);
                if (res.status) {

                    let stages = `<div class="stageSection">`;
                    if (res.data.pm.ProjectStage) {
                        if (res.data.stages.length > 0) {
                            $.each(res.data.stages, function (i, d) {
                                stages += `<label class="form-label"><a href="stage2">Stage ${i + 1}</a></label>
<div class="row">
    <div class="col-sm-4 mb-2">
        <label class="form-label fw-bolder">Key</label>
        <br />
        <span>${d.KeyPoint}</span>
    </div>
    <div class="col-sm-4 mb-2">
        <label class="form-label fw-bolder">Pdf Document</label>
        <br />
       <a href="${d.Document_Url}"><span>Document</span> <i class="fa-regular fa-file-pdf"></i></a>
    </div>
    <div class="col-sm-4 mb-2">
        <label class="form-label fw-bolder">Completion Date</label>
        <br />
        <span>${d.CompletionDatestring}</span>
    </div>
    <hr />
</div>`;
                            });
                            stages += "</div>";
                        }
                    }
                    let ExternalProject = ""
                    if (res.data.pm.ProjectType === "External") {
                        ExternalProject = `<div class="row">
    <div class="col-sm-4 mb-2">
        <label class="form-label fw-bolder">Department Name</label>
        <br />
        <span>${res.data.pm.ProjectDepartment}</span>
    </div>
    <div class="col-sm-4 mb-2">
        <label class="form-label fw-bolder">Contact Person</label>
        <br />
        <span>${res.data.pm.ContactPerson}</span>
    </div>
    <div class="col-sm-4 mb-2">
        <label class="form-label fw-bolder">Address</label>
        <br />
        <span>${res.data.pm.Address}</span>
    </div>
    <hr />
</div>`;
                    }

                    let subOrdinates = "";
                    if (res.data.SubOrdinate.length > 0) {
                        $.each(res.data.SubOrdinate, function (i, d) {
                            subOrdinates += ` <li>${d.Name}(${d.EmpCode})</li>`;
                        })
                    }
                    let all_element = `<div class="row">
     <div class="col-sm-4 mb-2">
         <label class="form-label fw-bolder">Title</label>
         <br />
         <span>${res.data.pm.ProjectTitle}</span>
     </div>
     <div class="col-sm-4 mb-2">
         <label class="form-label fw-bolder">Project Manager</label>
         <br />
         <span>${res.data.pm.ProjectManager}</span>
     </div>
     <div class="col-sm-4 mb-2">
         <label class="form-label fw-bolder">Assign Date</label>
         <br />
         <span>${convertToDate(res.data.pm.AssignDate)}</span>
     </div>

     <hr />
 </div>
 <div class="row">
     <div class="col-sm-4 mb-2">
         <label class="form-label fw-bolder">Start Date</label>
         <br />
         <span>${convertToDate(res.data.pm.StartDate)}</span>
     </div>
     <div class="col-sm-4 mb-2">
         <label class="form-label fw-bolder">Completion Date</label>
         <br />
         <span>${convertToDate(res.data.pm.CompletionDate)}</span>
     </div>
     <div class="col-sm-4 mb-2">
         <label class="form-label fw-bolder">Project Type</label>
         <br />
         <span>${res.data.pm.ProjectType}</span>
     </div>

     <hr />
 </div>
 ${res.data.pm.ProjectType === "External" ? ExternalProject : ""}
 <div class="row">
     <div class="col-sm-4 mb-2">
         <label class="form-label fw-bolder">Budget</label>
         <br />
         <span>${res.data.pm.ProjectBudget}</span>
     </div>
     <div class="col-sm-4 mb-2">
         <label class="form-label fw-bolder">Sub-Ordinate</label>
         <ul class="d-flex list-unstyled justify-content-between m-0">
             ${subOrdinates}
         </ul>
     </div>
     <div class="col-sm-4 mb-2">
         <label class="form-label fw-bolder">Stage</label>
         <br />
         <span>${res.data.pm.ProjectStage ? "Yes" : "No"}</span>
     </div>
     <hr />
 </div>
 ${res.data.pm.ProjectStage ? stages : ""}

     <div class="row">
         <label class="form-label fw-bolder">Description</label>
         <p>${res.data.pm.ProjectDescription}</p>
     </div>` ;

                    function convertToDate(timestamp) {
                        const parsedTimestamp = parseInt(timestamp.match(/\d+/)[0], 10);
                        const date = new Date(parsedTimestamp);
                        return date.toLocaleDateString();
                    }

                    $("#viewProjectModalBody").html(all_element);
                    $("#viewProjectModal").modal("show");
                    Swal.close();
                }
            },
            error: function (res) {

            },
            complete: function (res) {

            }
        })
    })

    $(document).ready(function () {



        $("#searchProjectBy").on("keyup", function () {
            var table = $("#AllProjectList").DataTable();
            table.column(2).search(this.value).draw()
        });
        $("#filterProjectStatus").on("change", function () {
            var table = $("#AllProjectList").DataTable();
            table.column(10).search(this.value).draw()
        })
        $("#searchByProjectManager").on("change", function () {
            var table = $("#AllProjectList").DataTable();
            table.column(3).search(this.value).draw()
        })

        $('#AllProjectList').DataTable({
            deferRender: false,
            layout: {
                topStart: {
                    buttons: ['excel', 'pdf']
                }
            },
            drawCallback: function () {
                feather.replace();
            }
        });
        feather.replace();
    });


    $(".monthlyReport").click(function () {
        let prjType = $(this).attr("data-prjType");
        let prjId = $(this).attr("data-prjId");
        if (prjType === "External") {
            loadExternalProjectDetail(prjId)

        } else if (prjType === "Internal") {
            loadInternalProjectDetail(prjId);

        }
    });

    function loadInternalProjectDetail(prjId) {
        if (prjId != null && prjId > 0) {
            debugger
            $.ajax({
                url: "/api/getWeeklyUpdate",
                type: "get",
                data: { projectId: prjId },
                success: function (res) {
                    console.log('Utilealgsas',res);
                    if (res.status) {
                        $("#internalProjectTbody").empty();
                        let index = 1;
                        $.each(res.data, function (i, d) {
                            $("#internalProjectTbody").append(
                                `
                                <tr>
                                    <td>${index++}</td>
                                    <td>${d.projectName}</td>
                                    <td>${d.unit}</td>
                                    <td>${d.annual}</td>
                                    <td>${d.monthEnd}</td>
                                    <td>${d.reviewMonth}</td>
                                    <td>${d.MonthEndSequentially}</td>
                                    <td>${d.StateBeneficiaries}</td>
                                    <td>${d.comments}</td>
                                </tr>
                                `
                            )
                        })

                        $("#viewMonthlyReportInternalPrj").modal("show")
                    } else {
                        Swal.fire({
                            title: 'Warning',
                            text: 'Data not found !',
                            icon: 'warning'
                        });
                    }
                },
                error: function (xhr) {
                    console.log(xhr);
                    Swal.fire({
                        title: "Warning",
                        text: xhr.responseJSON.StatusCode == 404? xhr.responseJSON.message:"Server issue occured. Please try again!",
                        icon: "warning",
                        didClose: () => {
                            location.reload(true)
                        }
                    })
                }
            });
        }
    }


    function loadExternalProjectDetail(prjId) {
        $.ajax({
            url: "/api/getFinancialReport",
            type: "get",
            data: { projectId: prjId },
            success: function (res) {
                console.log(res)
                if (res.status) {
                    $("#externalPrjTbody").empty();
                    let index = 1;
                    $.each(res.data, function (i, d) {
                        $("#externalPrjTbody").append(
                            `
                      <tr>
                          <td>${index++}</td>
                          <td>${d.projectName}</td>
                          <td>${d.totalBudget}</td>
                          <td>${d.department}</td>
                          <td></td>
                          <td></td>
                          <td>${d.aim}</td>
                          <td></td>
                          <td>${d.month_aim}</td>
                          <td>${d.completeInMonth}</td>
                          <td>${d.departBeneficiaries}</td>
                          <td>${d.description}</td>
                      </tr>
                      `
                        )
                    })

                    $("#viewMonthlyReportExternalPrj").modal("show")
                }
            },
            error: function () {
                Swal.fire({
                    title: "Server issue !",
                    text: "Some issue occured. Please try after sometime !",
                    icon: "error",
                    didClose: () => {
                        location.reload(true)
                    }
                })
            }
        });
    }

    $("#printMonthlyRprt").click(function () {

        Swal.fire({
            title: "Please wait !",
            text: "Processing your request....",
            allowOutsideClick: false,
            allowEscapeKey: false,
            didOpen: () => {
                Swal.showLoading();
            }
        })
        var formContent = document.getElementById("formToPrintMonthlyRprt").outerHTML;

        var style = `
       <link href="${window.location.origin}/assets/css/ExternalPrjStyle.css" rel="stylesheet" type="text/css"/>
       <link href="${window.location.origin}/assets/css/style.css" rel="stylesheet" type="text/css"/>
       `;
        let htmlContentToPdf = `<html><head>${style}</head><body><div>${formContent}</div></body></html>`;

        fetch("/login/ExportHtmlToPdf", {
            method: "POST",
            headers: {
                "Content-Type": "application/json"
            },
            body: JSON.stringify({ HtmlText: htmlContentToPdf }) // Ensure case matches C# model
        })
            .then(response => {
                if (!response.ok) {
                    throw new Error("Failed to generate PDF");
                }

                // Extract filename from Content-Disposition header
                const contentDisposition = response.headers.get("Content-Disposition");
                let fileName = "Generated.pdf"; // Default fallback filename

                if (contentDisposition) {
                    const match = contentDisposition.match(/filename="?([^"]+)"?/);
                    if (match && match[1]) {
                        fileName = match[1];
                    }
                }

                return response.blob().then(blob => ({ blob, fileName }));
            })
            .then(({ blob, fileName }) => {
                Swal.fire({
                    title: "Successfully !",
                    text: "Reinbursement pdf generated successfully.",
                    icon: "success"
                })
                const link = document.createElement("a");
                const url = URL.createObjectURL(blob);
                link.href = url;
                link.download = fileName;
                document.body.appendChild(link);
                link.click();
                link.remove();
                URL.revokeObjectURL(url); // Free up memory
            })
            .catch(error => {
                Swal.fire({
                    title: "Sorry !",
                    text: "Some issue occured while processing your request.",
                    icon: "error"
                })
                console.error("Error:", error)
            });

    })


    $("#printFinancialRprt").click(function () {

        Swal.fire({
            title: "Please wait !",
            text: "Processing your request....",
            allowOutsideClick: false,
            allowEscapeKey: false,
            didOpen: () => {
                Swal.showLoading();
            }
        })
        var formContent = document.getElementById("formToPrintFinancialRprt").outerHTML;
        var style = `
       <link href="${window.location.origin}/assets/css/ExternalPrjStyle.css" rel="stylesheet" type="text/css"/>
       <link href="${window.location.origin}/assets/css/style.css" rel="stylesheet" type="text/css"/>
       `;
        let htmlContentToPdf = `<html><head>${style}</head><body><div>${formContent}</div></body></html>`;

        fetch("/login/ExportHtmlToPdf", {
            method: "POST",
            headers: {
                "Content-Type": "application/json"
            },
            body: JSON.stringify({ HtmlText: htmlContentToPdf }) // Ensure case matches C# model
        })
            .then(response => {
                if (!response.ok) {
                    throw new Error("Failed to generate PDF");
                }

                // Extract filename from Content-Disposition header
                const contentDisposition = response.headers.get("Content-Disposition");
                let fileName = "Generated.pdf"; // Default fallback filename

                if (contentDisposition) {
                    const match = contentDisposition.match(/filename="?([^"]+)"?/);
                    if (match && match[1]) {
                        fileName = match[1];
                    }
                }

                return response.blob().then(blob => ({ blob, fileName }));
            })
            .then(({ blob, fileName }) => {
                Swal.fire({
                    title: "Successfully !",
                    text: "Reinbursement pdf generated successfully.",
                    icon: "success"
                })
                const link = document.createElement("a");
                const url = URL.createObjectURL(blob);
                link.href = url;
                link.download = fileName;
                document.body.appendChild(link);
                link.click();
                link.remove();
                URL.revokeObjectURL(url); // Free up memory
            })
            .catch(error => {
                Swal.fire({
                    title: "Sorry !",
                    text: "Some issue occured while processing your request.",
                    icon: "error"
                })
                console.error("Error:", error)
            });

    })


</script>


