
@{
    ViewBag.Title = "Reimbursement_Report";
    Layout = "~/Views/Shared/_AccountLayout.cshtml";
    var empData = ViewData["totalProjectManager"] as List<RemoteSensingProject.Models.Admin.main.Employee_model>;
    var reportData = ViewData["totalReinursementReport"] as List<RemoteSensingProject.Models.Admin.main.AdminReimbursement>;
}

<style>
    .dt-button {
        font-size: 14px !important;
        padding: 10px 20px !important;
        border-radius: 4px !important;
        border: none !important;
        cursor: pointer !important;
        margin-right: 5px !important;
        display: inline-block !important;
        font-weight: 600 !important;
        transition: all 0.3s ease-in-out !important;
    }

        .dt-button.buttons-excel {
            background-color: #3b9f42 !important; /* Excel-like green */
            color: white !important;
        }

            .dt-button.buttons-excel:hover {
                background-color: #2a7c2f !important; /* Darker green on hover */
            }

        /* PDF button (Red) */
        .dt-button.buttons-pdf {
            background-color: #e74c3c !important; /* PDF-like red */
            color: white !important;
        }

            .dt-button.buttons-pdf:hover {
                background-color: #c0392b !important; /* Darker red on hover */
            }

    /* Additional styling for the buttons wrapper */
    .dataTables_wrapper .dt-buttons {
        margin-bottom: 10px !important;
    }

    .dt-search {
        float: right;
        margin-bottom: 5px !important;
    }

    @@media print {
        .container {
            max-width: 100% !important;
            margin: 0 !important;
            padding: 0 !important;
        }

        .form-container {
            /* border: 1px solid #000; */
            padding: 15px !important;
            margin: 0 !important;
            width: 210mm; /* A4 width */
            min-height: 297mm; /* A4 height */
            font-size: 11px !important;
        }

        h4 {
            font-size: 16px !important;
        }

        h5 {
            font-size: 14px !important;
        }

        td, th {
            padding: 4px !important;
            font-size: 11px !important;
        }

        .bottom-section {
            padding: 10px !important;
        }

        .px-5 {
            padding-left: 10px !important;
            padding-right: 10px !important;
        }

        .mx-5 {
            margin-left: 10px !important;
            margin-right: 10px !important;
        }

        [class*="dots"] {
            min-width: 80px !important;
        }
        /* Ensure no page breaks within sections */
        .bottom-section, .table-responsive {
            page-break-inside: avoid;
        }
    }

    /* Adjust the table row height for better fit */
    td {
        height: 25px; /* Reduced from 30px */
    }

    .form-container {
        /* border: 2px solid #000; */
        background-color: #fff;
        max-width: 1000px;
        margin: 0 auto;
        font-size: 14px;
        font-family: "Times New Roman", Times, serif;
    }

    h4 {
        font-size: 18px;
        font-weight: bold;
    }

    h5 {
        font-size: 16px;
        font-weight: bold;
    }

    table {
        width: 100%;
        border-collapse: collapse;
    }

    th {
        font-size: 13px;
        padding: 8px;
        border: 1px solid #000;
        background-color: #f8f9fa;
    }

    td {
        height: 30px;
        padding: 6px;
        border: 1px solid #000;
        font-size: 13px;
    }

    .table-bordered {
        border: 1.5px solid #000;
    }

        .table-bordered > thead > tr > th,
        .table-bordered > tbody > tr > td {
            border: 1px solid #000;
        }

    /* Dotted line for form fields */
    [class*="dots"] {
        border-bottom: 1px dotted #000;
        display: inline-block;
        min-width: 100px;
    }

    @@media print {

        body {
            margin: 0;
            padding: 20px;
            font-size: 12px;
        }
    }
    /* Add these new styles to your existing CSS */
    .bottom-section {
        border: 1px solid #000;
        padding: 5px;
        margin-top: 0; /* Changed from 20px to 0 */
    }

        .bottom-section p {
            margin: 0px;
            padding: 0px;
        }

    table td {
        height: 25px;
    }

    .signature-section {
        border-top: 1px solid #000;
        margin-top: 20px;
        padding-top: 15px;
    }

    .officers-section {
        border-top: 1px solid #000;
        /* margin-top: 20px; */
        /* padding-top: 25px; Increased padding top */
    }

    .final-section {
        border-top: 1px solid #000;
        margin-top: 20px;
        padding-top: 15px;
        text-align: right; /* Align content to right */
    }

        .final-section p {
            margin-bottom: 5px;
        }

    .approved-text {
        position: relative;
        top: -10px;
        font-style: italic;
        font-size: 12px;
    }

    .form-header-text {
        width: 100%;
        padding: 0 15px;
    }

        .form-header-text p {
            margin-bottom: 8px;
        }

    .modal-dialog {
        margin-top: 10vh; /* You can adjust this value */
    }
</style>
<div class="card my-1">
    <div class="card-body">
        <div class="row">
            <div class="col-sm-4">
                <select class="form-select form-select-sm" id="filterByProjectManager">
                    <option value="" selected>Filter by Project Manager</option>
                    @if (empData.Any())
                    {
                        foreach (var item in empData)
                        {
                            <option>@item.EmployeeName (@item.EmployeeCode)</option>
                        }
                    }
                </select>

            </div>

            <div class="col-sm-4">
                <select class="form-select form-select-sm" id="filterByprojectType">
                    <option value="" selected>Filter by request type</option>
                    <option value="contigency">Contigency</option>
                    <option value="pi_adjustment">P.I. Adjustment</option>
                </select>

            </div>

            <div class="col-sm-4">
                <select class="form-select form-select-sm" id="fileterByProjectStatus">
                    <option value="" selected>Filter by status</option>
                    <option>Approved</option>
                    <option>Rejected</option>
                    <option>Pending</option>
                </select>

            </div>



        </div>
    </div>
</div>


<div class="card">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-hover" id="reibursementReportTable">
                <thead>
                    <tr>
                        <th>Sr. No</th>
                        <th>Project Manager</th>
                        <th>Type</th>
                        <th>Amount</th>
                        <th>Approve Amount</th>
                        <th>Status</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    @if (reportData != null)
                    {
                        int i = 1;
                        foreach (var item in reportData)
                        {
                            <tr>
                                <td>@i</td>
                                <td>@item.EmpName</td>
                                <td>@item.type</td>
                                <td>@item.amount</td>
                                <td>@item.approveAmount</td>
                                <td>
                                    @if (item.appr_status && !item.newRequest && item.status)
                                    {
                                        <span class="badge bg-success">Approved</span>
                                    }
                                    else if (!item.newRequest && (!item.appr_status || !item.status) )
                                    {
                                        <span class="badge bg-danger">Rejected</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-primary">Pending</span>

                                    }

                                </td>
                                <td>
                                    <a href="#" class="viewSanctionreciept" data-type="@item.type" data-userId="@item.userId" data-id="@item.id" data-chequeNum="@item.chequeNum" data-chequeDate="@item.chequeDate" data-apprAmt="@item.approveAmount">
                                        <i class="fa-solid fa-eye text-success"></i>
                                    </a>
                                </td>
                            </tr>

                            i++;
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<!--Modal-->
<div class="modal fade" id="ViewReinburseMentReciept" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-100" aria-labelledby="ViewReinburseMentRecieptLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-scrollable">
        <div class="modal-content">
            <div class="modal-header d-flex justify-content-between">
                <h1 class="modal-title fs-5" id="ViewReinburseMentRecieptLabel">Reimbursement Form </h1>
                <button type="button" class="btn-close" data-bs-dismiss="modal" id="closeModal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <div class="container" id="formToPrint">
                    <div class="form-container py-3">
                        <div class="text-center mb-3">
                            <h4>REMOTE SENSING APPLICATIONS CENTRE</h4>
                            <h5>UTTAR PRADESH</h5>
                            <div class="px-4 mt-1 d-flex justify-content-between">
                                <span>
                                    <p>
                                        <img src="~/assets/images/remote_sensing_logo.jfif" width="50px" />
                                    </p><b>S.F.A.O/Director</b>
                                </span>
                                <span>
                                    <p>
                                        <b>
                                            Name of Project/Centre
                                        </b>
                                    </p>
                                </span>
                            </div>
                        </div>

                        <div class="form-header-text w-100 text-center">
                            <p>
                                The following expenditure of Rs. <span class="dots"></span> have been incurred against Contingent/Advance of
                                Rs. <span class="dots"></span> taken as on <span class="dots"></span> kindly accord the sanction for adjustment/payment in accordance
                                with the enclosed contingent advance approval.
                            </p>
                        </div>


                        <div class="table-responsive mb-0 px-5">
                            <table class="table table-bordered mb-0">
                                <thead class="text-center">
                                    <tr>
                                        <th>S.N.</th>
                                        <th>Vr. No. & Date</th>
                                        <th>Particulars</th>
                                        <th>Items</th>
                                        <th>Purpose</th>
                                        <th>Amount Rs. P.</th>
                                    </tr>
                                </thead>
                                <tbody id="viewReinburseRecieptListBody">
                                </tbody>
                            </table>
                        </div>

                        <div class="bottom-section mt-0 mx-5">
                            <div class="row mt-0">
                                <div class="col-md-6">
                                    <p>Balance Rs. <span class="dots" id="apprAmttext"></span> (Rupees <span class="dots"></span>Only)</p>
                                    <p>paid vide cheque No. <span class="dots" id="chequeNumberText"></span> Date<span class="dots" id="chequeDatetext"></span></p>

                                </div>
                                <div class="col-md-6 text-end">
                                    <p>Signature <span class="dots"></span></p>
                                    <p>Name : <span class="dots"></span></p>
                                    <p>Designation : <span class="dots"></span></p>
                                </div>
                            </div>

                            <div class="officers-section">
                                <div class="row ps-3">
                                    <p class="pt-2">Checked for Rs. <span class="dots"></span> Rs.<span class="dots"></span></p>
                                    <div class="col-md-4 mt-3">
                                        <p>Asstt. Actt./Acctt./O.S. (Acctts)</p>
                                    </div>
                                    <div class="col-md-4 text-center mt-3">
                                        <p>A.O./S.F.A.O.</p>
                                    </div>
                                    <div class="col-md-4 text-end">
                                        <span class="approved-text">pending</span>
                                        <p>Sanctioning Authority</p>
                                    </div>
                                </div>
                            </div>

                            <div class="final-section pt-1 mt-0">
                                <p style="margin-right: 15px;">Passed for Rs. <span class="dots"></span> (RS. <span class="dots"></span>)</p>
                                <p style="margin-right: 15px;">Drawing & Disbursing Officer</p>
                            </div>
                        </div>

                    </div>
                </div>
                        <div class="text-end m-5">
                            <button class="btn btn-secondary py-1 rounded-0" id="printBtn">Print</button>
                        </div>
            </div>
        </div>
    </div>
</div>


<script>
    $(document).ready(function () {

    

        $("#reibursementReportTable").DataTable({
            layout: {
                topStart: {
                    buttons: ['excel', 'pdf']
                }
            }
        });

       
        $("#filterByProjectManager").on("change", function () {
            let table = $("#reibursementReportTable").DataTable();
            table.column(1).search($(this).val()).draw();
        })

        $("#filterByprojectType").on("change", function () {
            let table = $("#reibursementReportTable").DataTable();
            table.column(2).search($(this).val()).draw();
        })

        $("#fileterByProjectStatus").on("change", function () {
            let table = $("#reibursementReportTable").DataTable();
            table.column(5).search($(this).val()).draw();
        })


        $(".viewSanctionreciept").click(function () {
            Swal.fire({
                title: "Please wait !",
                text: "Processing your request ....",
                allowOutsideClick: false,
                allowEscapeKey: false,
                didOpen: () => {
                    Swal.showLoading();
                }
            });

            let type = $(this).attr("data-type");
            let userId = $(this).attr("data-userId");
            let Id = $(this).attr("data-id");
            let chequeNum = $(this).attr("data-chequeNum")
            let chequeDate = $(this).attr("data-chequeDate")
            let apprvAmt = $(this).attr("data-apprAmt")
            $.ajax({
                url: "/api/ViewReinbursementBytype",
                type: "Get",
                data: { "userId": userId, "type": type, id: Id },
                success: function (result) {
                    if (result.status) {
                        $("#apprAmttext").text(apprvAmt);
                        $("#chequeNumberText").text(chequeNum);
                        $("#chequeDatetext").text(chequeDate);
                        $("#viewReinburseRecieptListBody").empty();
                        Swal.close();
                        let totalAmout = 0;
                        let apprStatus = false;
                        let newStatus = false;
                        let element = "";
                        $.each(result.data, function (i, d) {
                            apprStatus = d.adminappr;
                            newStatus = d.newRequest
                            element += `
   <tr>
   <td>${i + 1}</td>
   <td>${d.vrNo} & ${convertToDate(d.date)}</td>
   <td>${d.particulars}</td>
   <td>${d.items}</td>
   <td>${d.purpose}</td>
   <td>${d.amount}</td>
   </tr>
   `
                            totalAmout += d.amount;
                        });

                        $(".approved-text").text(apprStatus && !newStatus ? "Approved" : !apprStatus && !newStatus ? "Rejected" : "Pending");
                        element += ` <tr>
    <td colspan="5" class="text-end"><b>Total</b></td>
    <td>${totalAmout}</td>
</tr>`;



                        $("#viewReinburseRecieptListBody").append(element);
                        $("#ViewReinburseMentReciept").modal("show");
                    }
                    else {
                        Swal.fire({
                            title: "Sorry !",
                            text: "Some issue occured !",
                            icon: "warning"
                        })
                    }
                },
                error: function () {
                    Swal.fire({
                        title: "Server issue !",
                        text: "Server is busy ! Please try after sometime.",
                        icon: "error",
                        didClose: () => {
                            location.reload(true);
                        }
                    })
                }
            })
        })


        function convertToDate(timestamp) {
            const parsedTimestamp = parseInt(timestamp.match(/\d+/)[0], 10);
            const date = new Date(parsedTimestamp);
            return date.toLocaleDateString();
        }

        document.getElementById("printBtn").onclick = function () {
            // Get the content of the form to be printed
            var formContent = document.getElementById("formToPrint").outerHTML;

            // Save the original body content
            var originalContent = document.body.innerHTML;
            var style = `<style>
            #formToPrint{
                font-family:'Times New Roman';
                            @@media print {
     .container {
         max-width: 80% !important;
         margin: 0 !important;
         padding: 1 !important;
     }

     .form-container {
         /* border: 1px solid #000; */
         padding: 15px !important;
         margin: 0 !important;
         font-size: 11px !important;
     }

     h4 {
         font-size: 16px !important;
     }

     h5 {
         font-size: 14px !important;
     }

     td, th {
         padding: 4px !important;
         font-size: 11px !important;
     }

     .bottom-section {
         padding: 10px !important;
     }

     .px-5 {
         padding-left: 10px !important;
         padding-right: 10px !important;
     }

     .mx-5 {
         margin-left: 10px !important;
         margin-right: 10px !important;
     }

     [class*="dots"] {
         min-width: 80px !important;
     }
     /* Ensure no page breaks within sections */
     .bottom-section, .table-responsive {
         page-break-inside: avoid;
     }
 }

 /* Adjust the table row height for better fit */
 td {
     height: 25px; /* Reduced from 30px */
 }

 .form-container {
     /* border: 2px solid #000; */
     background-color: #fff;
     max-width: 1000px;
     margin: 0 auto;
     font-size: 14px;
     font-family: "Times New Roman", Times, serif;
 }

 h4 {
     font-size: 18px;
     font-weight: bold;
 }

 h5 {
     font-size: 16px;
     font-weight: bold;
 }

 table {
     width: 100%;
     border-collapse: collapse;
 }

 th {
     font-size: 13px;
     padding: 8px;
     border: 1px solid #000;
     background-color: #f8f9fa;
 }

 td {
     height: 30px;
     padding: 6px;
     border: 1px solid #000;
     font-size: 13px;
 }

 .table-bordered {
     border: 1.5px solid #000;
 }

     .table-bordered > thead > tr > th,
     .table-bordered > tbody > tr > td {
         border: 1px solid #000;
     }

 /* Dotted line for form fields */
 [class*="dots"] {
     border-bottom: 1px dotted #000;
     display: inline-block;
     min-width: 100px;
 }

 @@media print {

     body {
         margin: 0;
         padding: 20px;
         font-size: 12px;
     }
 }
 /* Add these new styles to your existing CSS */
 .bottom-section {
     border: 1px solid #000;
     padding: 5px;
     margin-top: 0; /* Changed from 20px to 0 */
 }

     .bottom-section p {
         margin: 0px;
         padding: 0px;
     }

 table td {
     height: 25px;
 }

 .signature-section {
     border-top: 1px solid #000;
     margin-top: 20px;
     padding-top: 15px;
 }

 .officers-section {
     border-top: 1px solid #000;
     /* margin-top: 20px; */
     /* padding-top: 25px; Increased padding top */
 }

 .final-section {
     border-top: 1px solid #000;
     margin-top: 20px;
     padding-top: 15px;
     text-align: right; /* Align content to right */
 }

     .final-section p {
         margin-bottom: 5px;
     }

 .approved-text {
     position: relative;
     top: -10px;
     font-style: italic;
     font-size: 12px;
 }

 .form-header-text {
     width: 100%;
     padding: 0 15px;
 }

     .form-header-text p {
         margin-bottom: 8px;
     }
 .modal-dialog {
     margin-top: 10vh; /* You can adjust this value */
 }
</style>`;
            // Replace the body content with only the form content
            document.body.innerHTML = style + '<div>' + formContent + '</div>';

            // Trigger the print dialog
            window.print();

            // Restore the original body content after printing
            document.body.innerHTML = originalContent;
        };

    })
</script>